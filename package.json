{
  "name": "empty-test-repo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo containing a Node.js hello world app and a Spring Boot REST API.",
  "workspaces": [
    "apps/node-app"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "nx": "22.7.2"
  }
}
